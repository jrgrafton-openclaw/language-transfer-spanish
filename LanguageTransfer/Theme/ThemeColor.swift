import SwiftUI

enum ThemeColor {
    static let background = Color("Background")
    static let surface = Color("Surface")
    static let surfaceAlt = Color("SurfaceAlt")
    static let textPrimary = Color("TextPrimary")
    static let textSecondary = Color("TextSecondary")
    static let textTertiary = Color("TextTertiary")
    static let stroke = Color("Stroke")
    static let strokeEmphasis = Color("StrokeEmphasis")

    static let primary = Color("Primary")
    static let primaryEmphasis = Color("PrimaryEmphasis")
    static let primaryTint = Color("PrimaryTint")

    static let accent = Color("Accent")
    static let accentTint = Color("AccentTint")

    static let success = Color("Success")
    static let warning = Color("Warning")
    static let error = Color("Error")
    static let info = Color("Info")

    static let focusRing = Color("FocusRing")
}
